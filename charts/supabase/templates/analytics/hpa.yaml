{{- if and .Values.deployment.analytics.enabled .Values.autoscaling.analytics.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "supabase.analytics.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "supabase.analytics.fullname" . }}
  minReplicas: {{ .Values.autoscaling.analytics.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.analytics.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.analytics.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.analytics.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.analytics.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.analytics.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
