{{- if .Values.deployment.rustfs.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "supabase.fullname" . }}-test-rustfs
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
        - name: test-rustfs
          image: curlimages/curl:8.20.0
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              curl -sfo /dev/null \
                http://{{ include "supabase.rustfs.fullname" . }}:{{ .Values.service.rustfs.port }}/health
              echo "Sevice {{ include "supabase.rustfs.fullname" . }} is healthy."
      restartPolicy: Never
{{- end }}
