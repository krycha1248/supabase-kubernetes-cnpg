{{- if and .Values.deployment.meta.enabled .Values.autoscaling.meta.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "supabase.meta.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "supabase.meta.fullname" . }}
  minReplicas: {{ .Values.autoscaling.meta.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.meta.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.meta.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.meta.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.meta.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.meta.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
