{{- if and .Values.deployment.rest.enabled .Values.autoscaling.rest.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "supabase.rest.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "supabase.rest.fullname" . }}
  minReplicas: {{ .Values.autoscaling.rest.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.rest.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.rest.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.rest.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.rest.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.rest.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
