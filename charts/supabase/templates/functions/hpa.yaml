{{- if and .Values.deployment.functions.enabled .Values.autoscaling.functions.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "supabase.functions.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "supabase.functions.fullname" . }}
  minReplicas: {{ .Values.autoscaling.functions.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.functions.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.functions.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.functions.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.functions.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.functions.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
