{{- if and .Values.deployment.storage.enabled .Values.autoscaling.storage.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "supabase.storage.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "supabase.storage.fullname" . }}
  minReplicas: {{ .Values.autoscaling.storage.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.storage.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.storage.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.storage.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.storage.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.storage.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
